import { WorkflowPage } from "@/features/workflow/components/workflow-page";

export const metadata = {
  title: "Workflow — Clinical Trial",
};

export default function WorkflowRoute() {
  return <WorkflowPage />;
}
